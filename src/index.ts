export { ic, configureOutput, disable, enable, format } from "./ic";
export type {
  CallerPos,
  OriginalPos,
  IcConfiguration,
  PrefixFunction,
  OutputFunction,
  ArgToStringFunction,
} from "./types";
